## 1. Targeted Dictionary

- [x] 1.1 Implement targeted pattern detection (regex for long paths, URLs, identifiers) in `src/dictionary.rs`
- [x] 1.2 Implement dictionary building, replacement logic, and `@dict` header emission
- [x] 1.3 Integrate dictionary pass into engine pipeline (after grammar strip, before L3/L4)
- [x] 1.4 Add `dictionary` to `Config`, `-d`/`--dictionary` to CLI
